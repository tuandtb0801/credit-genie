# OpenAI API key powering the reasoning agents (Eligibility, Affordability, Risk, Explanation).
# Without it the backend still runs: LLM stages degrade to deterministic rules-only fallbacks.
OPENAI_API_KEY=

# Optional: override the reasoning model (default: gpt-5.1).
# CREDIT_GENIE_MODEL=gpt-5.1
